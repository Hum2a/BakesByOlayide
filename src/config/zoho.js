const zohoConfig = {
  email: process.env.REACT_APP_ZOHO_EMAIL,
  password: process.env.REACT_APP_ZOHO_EMAIL_PASSWORD,
  smtpHost: 'smtp.zoho.com',
  smtpPort: 587,
  secure: true
};

export default zohoConfig; 