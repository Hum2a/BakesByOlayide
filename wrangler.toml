# Cloudflare Pages — must match the Pages project name in the target account (see CLOUDFLARE.md).
name = "bakesbyolayide"
compatibility_date = "2024-11-01"
pages_build_output_dir = "build"

# Non-secret defaults (override in dashboard per environment)
[vars]
# CORS_ORIGINS = "https://preview-xxx.pages.dev"
